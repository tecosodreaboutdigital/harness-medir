*Read this in [Português](NEXT-STEPS.pt.md) · [Español](NEXT-STEPS.es.md).*

# Next steps

In order. Every item carries its done criterion.

---

## Completed on 30 August 2026

**Public GitHub repository created**, at `github.com/tecosodreaboutdigital/harness-medir`. Initial commit with the 23 files that existed at the time, no sensitive data. LICENSE (MIT) added inside what was then `skills/levantando-briefing/`, consistent with what the skill's README already declared. No licence at the root, because the articles remain all rights reserved.

**Compact guide completely rewritten.** See the matching entry in `STATUS.md` for detail. Done criterion met: seventeen six-field entries, every tool verified in `sources/inventory.md` (three new sources verified in that round: Semgrep, LangGraph, GitHub Spec Kit with a direct link), and every MEDIR step carrying a recorded critique.

**Part 2 translated into English and Spanish**, in the same file, with the selector. `build/build_p2.py` rewritten for trilingual assembly, in the same pattern `build/build_all.py` once used: extracts the PT body from the live file (which is the source of truth, not `build/body_p2_pt.html`, which had drifted out of date), assembles EN and ES from `build/body_p2_en.html` and `build/body_p2_es.html`, prefixes everything by language via `scope()`. All three buttons work and there is no broken anchor. As a side effect, the language-switching JavaScript in `harness-p1.html` and `harness-p2.html` gained anchor-based routing (`#en-opening` selects the right tab before scrolling), so a cross-link between the pieces in English or Spanish would not always land on the Portuguese tab.

**Thirty third-party skills installed for use on this project**, five collections (superpowers in full, a curated selection of twelve from mattpocock/skills, c4-skills in full, the Karpathy-inspired guide, ai-slop-cleaner), all MIT, all audited against the compact guide's own checklist before installing. They live in `.claude/skills/`, outside version control. Full documentation, with a real-usage log growing per session, in `TOOLS.md`. Credit visible in the footer of `harness-p1.html`, `harness-p2.html` (three languages) and `harness-toolkit.html`.

**The skill separated into its own repository**, at what was then `github.com/tecosodreaboutdigital/levantando-briefing`, public, MIT, in the same pattern as the other skills cited in the compact guide. Removed from `skills/` inside harness-medir, which now only points to it.

**Project log created**, `docs/logbook.html`, trilingual, generated in two steps: `build/generate_logbook_metrics.py` reconstructs the real series (words published per `git show` on each commit, lines in `build/` and the governance documents, tokens summed from the real `usage` of every message in the session's `.jsonl` transcript, attributed to the chronologically next commit) and writes `docs/assets/logbook-metrics.json`; `build/build_logbook.py` assembles the page from that JSON, never written by hand. Two stacked SVG charts, same X axis, no dual Y axis. Six real milestones recorded (the repository's full history so far, not a sample), plus whatever had not closed a milestone yet.

**GitHub Pages enabled**, publishing the repository at `tecosodreaboutdigital.github.io/harness-medir`. `.nojekyll` added to serve the HTML files as they are, with no Jekyll processing. Without it, no article was actually readable as a web page there, only as source code in GitHub's viewer.

**English-primary restructuring across both public repositories.** English became the primary production language, decided partway through this session. Every governance document renamed and rewritten English-first (`PADROES.md` → `STANDARDS.md`, `ESTADO.md` → `STATUS.md`, `PROXIMOS-PASSOS.md` → `NEXT-STEPS.md`, `FERRAMENTAS.md` → `TOOLS.md`, `fontes/inventario.md` → `sources/inventory.md`), each with a Portuguese and Spanish translation alongside it. `harness-caixa-de-ferramentas.html` → `harness-toolkit.html`, `docs/diario-de-bordo.html` → `docs/logbook.html`, matching `build/` scripts renamed and, for the toolkit, rewritten into a trilingual build with English as the default tab. `harness-p1.html`, `harness-p2.html` and `docs/logbook.html` had their default tab flipped from Portuguese to English, in both the built files and the scripts that regenerate them. A browser-language hint banner was added to all four trilingual pages (Portuguese or Spanish browsers get a dismissible offer to switch, anything else falls back to English silently). The skill repository was renamed `levantando-briefing` → `intake-briefing` and every one of its four files rewritten English-first with `.pt.md`/`.es.md` translations, plus a static language-navigation line at the top of each, since GitHub does not execute JavaScript inside rendered Markdown. See `STATUS.md` for the full detail and `STANDARDS.md`'s `Languages` section for the rule itself.

**Four externally recommended skills checked, one adopted.** `ponytail`, `no-ai-slop`, `taste-skill` and `impeccable` were compared on relevance, maintenance signal, contributor count and licence before any of them touched this repository. `ponytail` (117,000 stars, benchmarked, actively maintained) would have resolved `karpathy-guidelines`' own misattribution caveat but was set aside for now rather than adopted. `no-ai-slop` is a genuinely useful prose-cleanup skill whose banned-word list happens to include this project's own core term, "harness", and was also set aside pending a decision on how to except it. `taste-skill` was declined outright: its ten sub-skills read in exactly the hype register a project about evidence over opinion should not cite, and six contributors against 82,000 stars is a thin base. `impeccable` was adopted: Apache-2.0, 30 contributors, versioned (v4.1.2), derives from Anthropic's own frontend-design skill, and its 61 deterministic detector rules fit Inspect's own definition, evidence instead of opinion, applied to frontend design specifically. Installed as documentation only, `SKILL.md` and `reference/`, deliberately leaving out the `scripts/` tree its CLI needs, see `TOOLS.md`. Added as the compact guide's eighteenth entry, in Inspect, across all three languages, see `sources/inventory.md` for the verification record.

**Parts 3 and 4 preparation, the first three items of the working dossier's queue.** `docs/harness-p3-p4-briefing.pt.md` set the queue in its block D, and this round closed items 1 through 3. `README.md`, `STATUS.md`, `NEXT-STEPS.md` and `STANDARDS.md`, in all three languages, now describe four parts organised around a three-layer framework, build, operation, governance, crossed by the N0 to N3 tiers, instead of three parts plus two companions, and `STANDARDS.md` gained the `Diagrams` rule the dossier proposed: Mermaid first, the SVG derives and never replaces it. `sources/inventory.md` gained a new section carrying the dossier's 32 sources across seven research axes plus the Air Canada finding, original V or P status preserved, with an explicit note on the two that mark a real gap rather than a missing link: the rule of two's primary source, and the fact that no Brazilian precedent exists yet. Nine standalone SVG files, D1 to D9, were rendered into the new `diagrams/` directory from the dossier's Mermaid specifications, in English since this is new content, with Part 4's governance vocabulary (agent owner, certifier, auditor, area sponsor, receipt kept distinct from record) fixed at this stage so the diagrams and the eventual prose do not drift. See `STATUS.md` for the full detail and `diagrams/README.md` for the index.

**Diagrams D1 to D9 validated by rendering, and a unified series architecture.** The nine standalone SVGs were rendered for the first time (headless Chrome) and checked visually, closing the previous round's caveat. Five carried real coordinate bugs (connector lines cutting through box text in D3, a duplicate connector in D4, a caption overflowing the canvas in D5, a line crossing a box and a curve clipping another in D7, two overlapping columns in D9), all fixed in the SVG source; D1, D2, D6 and D8 had none. Each SVG gained a matching `.png` (2x, opaque white background) for Medium publication, since Medium's editor does not render pasted inline SVG. Separately, the series gained the top navigation the reader was missing: a single shared `.topbar` component, centred, sticky, reactive to the language selector (`SERIES` object plus `setSeries()` inside every page's `set()`), replacing the inconsistent, Portuguese-only, non-sticky bar that previously existed only on `harness-p1.html`. The per-article glossary and sources sections were retired in favour of two new shared, trilingual pages, `harness-glossary.html` and `harness-sources.html`, consolidating and deduplicating every term and citation from parts 1 and 2; every in-body glossary link and citation across the whole series now points there. `harness-p1.html`, `harness-p2.html` and `harness-toolkit.html` were retrofitted (and `build_p2.py` / `build_toolkit.py` updated to reproduce the same bar on the next regeneration, so the fix does not silently revert). Part 3's English body was drafted in full against this new pattern (see the next section) and wired into `harness-p3.html`; its Portuguese and Spanish tabs are an honest "translation in progress" stub, not fabricated text, pending the next session.

---

## Completed on 31 August 2026

**AGENTS.md and llms.txt created, the repository made operable by an AI agent, not only readable by one.** `AGENTS.md`, English only, at the repository root: the operating protocol for any AI agent or assistant acting on this repository, written in `STANDARDS.md`'s own skill-writing pattern (non-negotiable rule, red flags, verification steps, an explicit exit path when no network access exists, a Never section, honest limits). Its core rule: before installing, recommending or fetching any third-party skill this project curates, check the origin URL for whether it is still current, never present `sources/inventory.md`'s dated snapshot as a live state. `llms.txt` added at the root, publishing a discovery index for an agent that only fetched the site's URL. `README.md` gained a visible section, "This repository is also built to be operated", right after The thesis, plus a line at the top pointing to `AGENTS.md`, without touching the rest of the document, in all three languages. `harness-toolkit.html` section 11 and the top of `sources/inventory.md` each gained one sentence pointing an agent at the same protocol, in every language the page carries. Every insertion is additive: no existing paragraph of the README, the compact guide or the ledger was rewritten.

**The project log made visible in the README, with its own two charts.** `docs/logbook.html` used to appear as a single line in the file tree and nowhere else in `README.md`. A new section, "This project also inspects itself", placed right after the MEDIR cycle table so it reads as Inspect applied reflexively, embeds the log's own two real charts (words published and tokens consumed, cumulative by milestone, same X axis) as `docs/assets/logbook-words-published.png` and `docs/assets/logbook-tokens-consumed.png`, exported at 2x from the current `docs/logbook.html` markup via the same headless-Chrome screenshot method the D1 to D10 diagrams already use. Each caption states plainly that the embed is a snapshot as of the logbook's last regeneration, not a live view, and points to `docs/logbook.html` for the current numbers. Regenerate both PNGs whenever the logbook itself regenerates, or the README's snapshot drifts from the page it summarises.

**GitHub Pages checked for the first time, one broken link found and fixed.** The bare Pages URL, `tecosodreaboutdigital.github.io/harness-medir`, cited from `README.md`, `STATUS.md` and this file since it went live on 30 August, had never actually been opened, exactly the check this file's own `Publication` item had left pending. It 404s: `gh api repos/tecosodreaboutdigital/harness-medir/pages` confirms the site serves from the repository root, and no `index.html` existed there to answer a request for the bare directory. Fixed at the source rather than in each citation: a minimal `index.html` now sits at the root and redirects to `harness-p1.html`, the series' natural entry point, which already carries the full navigation bar and the browser-language hint. Every reference to the bare Pages URL across the repository resolves correctly now, with no text changed in any of them.

**Multi-tool installation for `intake-briefing` documented directly in this repository, not only in the skill's own.** `README.md` gained a full `Installation` section, all three languages, deliberately duplicating the skill repository's own already-verified instructions rather than only linking out: personal skill clone, `.agents/skills/` for Cursor, Codex CLI and Google Antigravity, Google AI Studio's Playground by mounting the repository or pasting the files, and a Claude Code plugin from its own marketplace. Each of the four vendor claims, plus the open Agent Skills format itself, was checked independently against that vendor's own documentation before writing it here, not only inherited from the skill repository's 31 August verification note. `harness-toolkit.html`'s compact-guide card for `intake-briefing` gained the same multi-tool detail inside its `How to start in 15 minutes` field, all three languages, the other five fields of the six-field format untouched. The `intake-briefing` repository itself had, separately, already been packaged with `AGENTS.md`, `llms.txt`, `.claude-plugin/` and `briefings/`; the stale local copy at `.claude/skills/intake-briefing/`, the risk `TOOLS.md` had already named, was resynced to match. The duplication is deliberate, not an oversight of the project's usual single-shared-page rule: the goal is a repeatable installation pattern for any future repository on this account about an agent, an automation or a skill, each one self-sufficient even while pointing at the others.

**A logbook icon added to every page's topbar, closing the one navigation gap the GitHub Pages check surfaced.** A small step-line SVG icon, echoing the log's own charts, now sits in the shared `.topbar` on all eight HTML pages, the four parts, the compact guide, the glossary, the sources page, and `docs/logbook.html` itself, the last one gaining the full topbar for the first time. It links to the project log with a `title`-attribute tooltip that translates with the language selector, driven by the same `SERIES`/`setSeries()` mechanism as every other topbar item, extended with one small branch: an icon element gets its `title` set on a language switch, not its `textContent`, so the SVG survives the swap. Two real bugs found and fixed along the way, neither caused by this round but both exposed by it. `build_p2.py`'s language-hint CSS injection was not idempotent, since the script is self-referential, it extracts its own shell from the current `harness-p2.html`, so every prior run had silently prepended one more copy of the same five-line CSS block; `harness-p2.html` alone had accumulated seven copies before this round, the other regenerated pages had inherited three to six each. Fixed by making the injection conditional, then stripping every page back to one copy through a full regeneration cascade. Separately, a bare `data-icon` HTML attribute resolves to an empty string in `dataset.icon`, which JavaScript treats as falsy, so the icon's tooltip silently failed to translate on a language switch even though its link target did; fixed by giving the attribute an explicit value, `data-icon="1"`, verified afterward with a real headless-browser click-through test, not just a visual read. A third near-miss caught in the same round: the `intake-briefing` compact-guide card's multi-tool installation text, added earlier by hand directly to `harness-toolkit.html`, had never been written into its real source, `build/body_toolkit_*.html`, so the first topbar regeneration silently discarded it; caught by grepping the regenerated file for a known phrase, then written into the correct source and rebuilt. `STANDARDS.md`'s Cross-navigation section, all three languages, now documents the icon and states the topbar's coverage is complete across every HTML page, no exception left.

**A real build-drift bug found by verification and fixed: `harness-sources.html` was one regeneration away from silently erasing two commits of fact-checking.** Checked by actually running every live build script and diffing the result against the committed files, the same discipline this project applies to its sources, not by reading `NEXT-STEPS.md` and trusting it. `build/body_sources_en.html`, `_pt.html` and `_es.html` had not been touched since 31 August 11:22 (`d34b9d1`), but the two citation-correction commits later that day (`691efb3`, `441f26b`) edited `harness-sources.html` directly, without updating its declared source of truth in `build/`. Running `build_sources.py` as it stood would have reverted eighteen corrected citations back to their wrong or unlocated state (the LangChain and HarnessX sources, the Veza and Barracuda primary sources, the ClawHavoc attribution, the rule of two, and more), with no warning: the script's own broken-link check only verifies that anchor IDs resolve, not that content matches. Fixed by extracting the three current, correct `<main>` bodies from the live `harness-sources.html`, de-scoping their language-prefixed IDs, and writing them back into `build/body_sources_*.html`, the same self-healing pattern `build_p2.py` already uses for its PT body. Verified by a second full regeneration pass producing zero further diff. Two smaller, harmless copies of the same root cause were fixed alongside it: `harness-glossary.html` and `harness-toolkit.html` were both missing the `.src` citation-tooltip CSS class added to the four parts on 31 August, invisible today since neither page uses a `.src` span yet, but silently reappearing on their next rebuild regardless, since both pull their shell from the same `harness-p2.html`. `build/README.md` gained a paragraph naming this exact risk, so a future hand-edit to `harness-sources.html`, `harness-glossary.html` or `harness-toolkit.html` is not lost the same way again.

---

## Completed on 10 September 2026

**Reader feedback round: eight candidate skills reviewed, four adopted, plus a gap search on architecture, privacy and security.** A reader sent a list of eight candidate skill repositories to compare against the existing toolkit, and asked for a search on what the toolkit was missing on software architecture, data privacy and information security. Five background agents verified each candidate against raw file content (not marketing README) and cross-checked against what was already installed, following the same audit discipline `TOOLS.md` already applies. Two of the eight were already covered (the Karpathy-inspired guide was the exact same source already cited; a UI-design skill matched something already active globally on the operator's machine, out of this project's scope). Adopted, after the operator's decision: `humanizer` (blader/humanizer, in full), three of the seventeen skills in `muratcankoylan/Agent-Skills-for-Context-Engineering` (`harness-engineering`, `multi-agent-patterns`, `tool-design`), `ai-act-skill` (morellid/ai-act-skill, in full), and `threat-modeling` (one skill out of `rjmurillo/ai-agents`). All four installed locally, audited (standard-library-only Python, no undeclared network calls; the one hit, a `sandbox.exec()` call, is a didactic code sample in a reference document, not an executable script), and documented in `TOOLS.md` in all three languages. The compact guide gained six-field entries for all four, in the sections that fit their actual content (Equip for the context-engineering bundle, Reinforce for `humanizer`, Secure for `threat-modeling`, Govern for `ai-act-skill`), all three languages, with `build/body_toolkit_*.html` kept in sync and the build rerun to confirm zero broken anchors. `sources/inventory.md` gained the citation rows for all four, plus a dated round documenting the four repositories reviewed and set aside as cite-only or a poor fit (`taste-skill`, `frontend-slides`, `diagram-design` cite-only; `Understand-Anything` and `stop-slop` discarded), the architecture and privacy/security candidates researched but not adopted, and one real security finding worth naming on its own: `Understand-Anything` installs via a `curl` piped into `bash` and ships a hook whose own instructions tell the agent not to ask the user for confirmation before acting, exactly the pattern this project's own "before installing anything" checklist exists to catch. A separate repository the reader asked about directly, `deepseek-ai/deepseek-harness`, already cited in this project, was re-verified against its own `docs/architecture.md`: the existing "append-only log" description held up under a deeper read (`core/session` is documented as owning "the append-only `SessionEvent` log and in-memory store"), so no correction was needed, a case of double-checking confirming an existing claim rather than finding a new error.

**A real, pre-existing STANDARDS.md violation found and fixed in the compact guide, and a much larger instance of the same bug found project-wide but not yet fixed.** While adding the new entries above, a stray em dash surfaced in the compact guide's own diagnosis table (all three languages), a direct violation of `STANDARDS.md`'s own "most-violated and most important rule." Fixed at the source (`build/body_toolkit_*.html`) and rebuilt. A project-wide scan run immediately after, prompted by finding this one instance, turned up the same character across nearly every published HTML page and governance document, dozens of files. Tracked below as its own item rather than attempted in the same session that found it, since a fix at that scale needs the same file-by-file care this round gave the toolkit, not a rushed pass.

---

## Completed on 11 September 2026

**Two of this project's own mechanisms reused in an unrelated, smaller project, and three real bugs it surfaced fixed here as a result.** The author adapted `build/generate_logbook_metrics.py` and the glossary's alphabetical-order discipline directly into a confidential, single-document, no-build client project, then wrote up what that reuse found. Every finding was independently reverified against this repository's own data before anything was changed, not taken on faith from the write-up.

1. **Token double-counting in the project log, confirmed and fixed.** `load_usage_events()` summed every transcript line carrying a `usage` field, with no dedup. A single assistant message produces more than one line (a thinking block, then text or a tool call), and each one carries that message's cumulative usage, repeated. Verified directly against this machine's own 15 session transcripts before touching any code: 5,663 usage-bearing lines collapse to 2,610 unique `message.id`s, an inflation factor of about 2.17x, consistent across every file. Fixed by deduplicating on `message.id`, keeping each id's last, most complete occurrence. The token totals in every earlier milestone's own prose in `docs/logbook.html` (written by whichever generator existed at the time) are left exactly as they were, an honest record of what was believed then; the table columns and cumulative totals are recalculated by this same regeneration and now run roughly half of what they showed before, about 977.8 million tokens transacted as of this session's last commit before the regeneration, against roughly 2,005.3 million under the old, doubled count two commits earlier.
2. **A local file path, with this machine's Windows username in it, published in a versioned public JSON, fixed going forward and scrubbed from history.** `docs/assets/logbook-metrics.json`'s `session_files` field stored each transcript's full absolute path. Not a credential, but personal information with no reason to be there. `generate_logbook_metrics.py` now stores `os.path.basename()` only. Because the old paths had already been public for weeks across dozens of commits, git history itself was rewritten (via `git-filter-repo`) to remove the leaked paths from every prior commit that carried them, not just corrected going forward, followed by a force-push; a full mirror backup was taken first.
3. **Two Portuguese glossary entries found out of order, live in the published page for eleven days, plus a standing check so it does not happen silently again.** The 31 August terminology revision renamed two glossary terms (sensor to verificador, dono do agente to proprietário do agente) without moving either entry to its new alphabetical slot; English and Spanish were unaffected since neither term changed there. Fixed at the source (`build/body_glossary_pt.html`) and rebuilt, zero broken anchors. `build/check_glossary_order.py` added: normalizes each term with `unicodedata` (NFKD, combining marks stripped) before comparing, so accented terms in Portuguese and Spanish collate correctly, and flags any adjacent pair out of order in any of the three languages. Documented in `build/README.md`, alongside a new note on what reusing `generate_logbook_metrics.py` elsewhere would require (the session-directory suffix, the hardcoded file lists, and item 1 above, all three not exposed as parameters today).

Two things the write-up also named as working as intended, kept here for the record rather than acted on: this project's own sizing rule (a harness has to be smaller than the failure surface it controls) held up as a real decision criterion on that smaller external project, which deliberately carried none of this repository's 38 skills or trilingual pipeline; and a single consolidated pre-publication checklist script on that same project caught, in seconds, a real navigation gap eight days of manual review had missed, the same "evidence, not opinion" argument this project makes about sensors.

**The one item left open above, closed the same day.** The README's two embedded logbook charts needed headless Chrome, unavailable at the time; Playwright (Python) was installed and pointed at the browser already on this machine (`channel="chrome"`, no separate Chromium download), which extracted the two chart SVGs directly out of the built `docs/logbook.html` and re-exported both at a native 2x device scale, 1400×520 pixels each, matching the previous exports' dimensions exactly. Both now show the corrected, sixty-three-milestone state; the README's alt text was updated to match in all three languages.

---

## Completed on 13 September 2026

**Two pieces of reader feedback, from other developers, about how this project holds up once an agent, not a human, is the one reading it inside someone else's project.** Session opened with no specific ask; before picking up the playbook (item 3, the only other known-pending item), the operator raised two points and asked for an analysis of both before deciding how to sequence them.

**The first: the project log's own build machinery should become a standalone skill, the way `intake-briefing` did.** Real signal behind this, not just an intuition: `build/generate_logbook_metrics.py`, the metrics engine, was already reused in an unrelated confidential client project on 11 September 2026 (see the entry above), and that reuse surfaced three real bugs fixed here as a direct result. `build/build_logbook.py` (922 lines, the trilingual page template, the shared topbar, the `SERIES` object) is a different claim, and never generalised in that same reuse. Decided: parked, not started, tracked as its own item, number 5 below, kept distinct from the playbook so the two are not conflated into one larger, less disciplined piece of work.

**The second: an agent dropped straight into a third-party project, pointed only at this repository's URL, cannot cleanly answer "what skills exist, what is each one for, how do I get one into my project."** Confirmed as real by reading the actual files, not assumed from the complaint: the answer was split across four documents in three different formats, `TOOLS.md`'s prose table, `sources/inventory.md`'s V/P ledger, `harness-toolkit.html`'s six-field human-facing cards organised by MEDIR step, and `AGENTS.md`'s verification protocol, which points at the other two but was never itself a catalogue. `TOOLS.md` already named its own version of this exact gap: "the four most recent [collections] are not yet [cited in the compact guide]."

**Closed the same day: `toolkit.json`, a generated, machine-readable manifest.** `build/generate_toolkit_manifest.py` derives it from `TOOLS.md`'s collections table and `sources/inventory.md`'s "Tools and skills" table, the same self-healing discipline as `generate_logbook_metrics.py`, never hand-edited. Eleven entries, the ten installed third-party collections plus `intake-briefing` as `kind: "own_skill"`, each carrying its role, the MEDIR step it grounds, its origin repository, a licence, its individual skill ids, and install paths for Claude Code (personal and project folder), Cursor, Codex CLI and Google Antigravity. Scope stated honestly in the manifest's own header: installed Agent Skills only, not every tool `sources/inventory.md` cites, Semgrep, Stryker and LangGraph are conventional software with no uniform "skill install path," forcing them into the same schema would assert something never verified. A `--check` mode hashes the two source tables and compares against what is committed, so editing `TOOLS.md` or `sources/inventory.md` without regenerating is caught the same way `build_sources.py`'s drift was caught on 31 August. `AGENTS.md`, `llms.txt`, `TOOLS.md`, `sources/inventory.md` and the file tree in `README.md` (all three languages) now point to it. `kind: "template"` is reserved in the schema, not yet populated: when the playbook (item 3) is consolidated, its seven templates register in this same manifest instead of reopening the design question later.

---

## 1. Write Part 3, done

**Status: written and live at `harness-p3.html`, all three languages.** The nine sections below are all in the body (`build/body_p3_en.html`, `_pt.html`, `_es.html`), with the five diagrams inline and fully translated (including every SVG label, checked by rendering each diagram standalone in all three languages, no overflow, no overlap), the matrix-of-authority and receipt-schema artefacts in the text, both honesty obligations honoured, and the Air Canada character arc closing in section 9. Word counts run close across the three (EN 5,131, PT 5,468, ES 5,676), consistent with a real translation rather than a summary. Kept as tracking record below.

Research is complete, see `sources/inventory.md`. The dossier's own closing section, block B's "What the research changes in the planned structure", revises the original ten-section sketch down to nine. Reconstructed here for tracking, confirm against the dossier before drafting:

1. Air Canada opens the piece: a real company argued in a real tribunal that its assistant was a separate legal person, answerable on its own, and lost
2. The director re-enters here, at N2, facing the first irreversible action, the contrast against Air Canada's argument is the point
3. The separation of powers, with the rule of two as its operational tool, the equivalent of Part 2's guides-and-sensors matrix
4. Identity and a named owner: who deployed it, what it is authorized to do, on whose behalf it is acting right now
5. When the order arrives inside the data
6. What needs to be logged, and what reversal actually means
7. A third-party skill is third-party code, closing the hook Part 2 left open
8. Legal obligations in two columns, Brazil and Europe, what already applies before what is still pending
9. Who answers for it, and where you are, the series' closing

Two honesty obligations the dossier flags explicitly: state that the Air Canada precedent is foreign, or a legal reader will discount the whole piece, and date the claim that Brazil's specific regulation for LGPD article 20 has not been published yet, because that sentence can age within months.

**Done when:** all three versions are ready, the character's arc closes, the piece works alone for a reader who has not read the previous ones, and the matrix-of-authority and receipt-schema artefacts it leaves behind are in the text. **Met, 30 August 2026.**

---

## 2. Write Part 4, done

**Status: fully trilingual and live at `harness-p4.html`, since 31 August 2026.** Nine sections, five diagrams inline (D6 to D10, D10 newly rendered for this piece, then translated and visually re-validated in all three languages before merging), the six-state life cycle table, the four-role table with the non-accumulation rule, the eight-indicator table with the honesty admission that the panel is synthesis, the SCHUFA-grounded gate-rejection-rate argument tying part 4 to part 3's legal ground, the CISO-reporting-line precedent for where the office sits, the seven-agents-and-a-spreadsheet constraint held through the warning section, and the closing table matching part 3's pattern. Word counts run close across all three (English 6,889, Portuguese 7,051, Spanish 7,377), consistent with a real translation rather than a summary. Ten new glossary terms added across all three languages; `harness-sources.html` gained a Part 4 section, 32 sources.

Research is complete, see `sources/inventory.md`'s Part 4 section and `docs/research-part4.pt.md`. Central thesis realised: the agent life cycle, states and not steps, distinguished from MEDIR with enough clarity that a reader cannot confuse the two.

Structure covered, in the order actually drafted:

1. Opens with the 20,225-account Instagram takeover, part 3's concentration failure mode at population scale
2. The director re-enters running six systems nobody has counted, unable to answer a board member's simplest question
3. The life cycle itself, six states from a versioned briefing to decommissioning, with the two transitions almost nobody implements, expired certification and no execution in the period, both leading to decommissioning
4. The four roles and the non-accumulation rule, the same separation of powers as Part 3, now on the organisational plane, sourced to the IIA's Three Lines Model
5. The eight indicators, two of which measure the governance's own quality rather than the agent's: the exception rate, and the gate's own refusal rate, closed by D10, the office's quarterly loop
6. Where the office sits, and why not in IT, presenting the realistic options with the cost of each
7. The warning: the antidote to reading as a vendor brochure is an explicit constraint running through the text, everything it proposes has to work in a company with seven agents and a spreadsheet
8. The closing argument: every platform governs inward, which is why the office has to be a function of the company, not a product it buys
9. Who answers, and where you stand: the director's arc closes across all four parts

**Done when:** all three versions are ready, the character's arc closes the series, the eight indicators are defined with a formula, and the text holds to the seven-agents-and-a-spreadsheet constraint throughout. **Met in full, 31 August 2026.**

---

## 3. Consolidate the playbook, done

Parked 31 August 2026, picked up and closed 13 September 2026, the same session that built `toolkit.json`. Not scoped for the content-writing session that immediately followed finishing the article series, decided deliberately: the four parts are the argument, the playbook is the toolkit derived from them, and building it right then would have blurred that boundary. This entry stayed as the explicit, deliberately parked record of what the playbook would contain until it was actually picked up.

Reuses the four parts and the guide, and adds seven operational templates that do not exist yet, each one traceable to the piece that already introduced its underlying concept, not invented fresh:

- Task-contract template, from the task contract in Part 1's Map step
- Skill template, derived from Part 2's three worked examples of the skill format
- Execution-receipt template, from Part 2's original receipt, extended in Part 3 with the rule-of-two answers and the reversal point
- Risk matrix by tier, from Part 3's matrix of authority (class of action, reversibility, authority required) crossed with Part 1's N0 to N3 tiers
- Tier diagnostic, questionnaire version, expanding Part 1's twelve-question checklist into a standalone instrument
- Rollout path from N0 to N3, the progression Part 1 only sketched
- Agent-registry template and certification-meeting-minutes template, from Part 4's life cycle states and four roles, the registry answering "how many exist" and the minutes formalising the certifier's act

D10, the office's own quarterly loop, is no longer a playbook candidate: it shipped inside Part 4 itself, closing the eight-indicators section. It can still open the playbook as a second appearance, legitimately, since it organises the whole document there rather than competing with itself.

**Done when it is actually picked up:** all seven templates exist, each cross-referenced back to the part and section that grounds it, and the playbook opens with D10 as its own organising diagram.

**Closed 13 September 2026.** All seven templates exist as eight real files in `playbook/` (the registry and the certification minutes ship separately, as planned above), each one naming, in its own text, the exact part and section it derives from, no invented concept among them: `task-contract.md` (Part 1's Map step), `skill-template.md` (Part 2's five-field skeleton and two worked examples), `execution-receipt.md` (Part 2's base receipt, extended with Part 3's rule-of-two answers and reversal point), `risk-matrix-by-tier.md` (Part 3's matrix of authority crossed with Part 1's tiers), `tier-diagnostic.md` (Part 1's twelve-question checklist, turned into a standing instrument), `rollout-path.md` (assembled from Part 1's gating rule and Part 2's firing-history signal, the one artefact that did not exist as a single quote in either part), `agent-registry.md` and `certification-minutes.md` (Part 4's life cycle, four roles and "a registry is a table, certification is a meeting with minutes"). `harness-playbook.html` explains and links each one, opening with D10, English complete, Portuguese and Spanish an honest translation-in-progress stub, the same pattern Part 3 and Part 4 used before they gained full translation. `playbook/README.md` is the machine- and human-readable index; each template is also registered in `toolkit.json` as `kind: "template"`.

**Picking this up required extending the shared `.topbar` to an eighth series item, across every existing page.** `harness-playbook.html` joining the series meant `SERIES_ORDER`, `FILES`, `LABELS_EN` and `EXISTS` needed a `'playbook'` entry in all seven build scripts that carry them (`build_p2.py`, `build_p3.py`, `build_p4.py`, `build_glossary.py`, `build_sources.py`, `build_toolkit.py`, `build_logbook.py`), plus a direct hand-edit to `harness-p1.html`, the one page with no working build script. `STANDARDS.md`'s own Cross-navigation rule states this coverage has to be complete across every HTML page, no exception, so this was not optional polish. All eight pages regenerated afterward with zero broken links.

---

## 4. Audit skill citations for consistency, and close the Part 3 and Part 4 gap, done

Raised 31 August 2026, from a direct question about one specific skill that exposed a broader pattern worth fixing deliberately, not one at a time by accident.

**The specific case that started it.** `research`, from the `mattpocock/skills` collection, is one of the twelve skills this project already installed, already audited (the collection passed `TOOLS.md`'s network/execution-pattern scan and licence check), and already marked **V** in `sources/inventory.md`. It is real, in use, and cleared. But it is never named anywhere in the project's own text: the compact guide's `mattpocock/skills` card spotlights a different skill from the same twelve (the one that forces a questioning session before execution) as its worked example, and Part 2's prose does not mention it either. This session used it directly, repeatedly, at real scale, to research and verify the primary-source gaps closed in the two citation-correction rounds earlier on 31 August: it held up well across every use, its output was consistently well-sourced and saved to a sensible location, with one minor inefficiency worth a note if it gets written up, its own instruction to "spin up a background agent" causes one redundant layer of delegation when it is invoked from inside a call that is already a background agent. Being installed and audited at the collection level is not the same thing as being individually verified and cited. Give `research` its own line, in the compact guide and, if it fits the argument naturally, in Part 2's prose, the same way `ai-slop-cleaner`, `holdfast` and `sensors-cli` each get one.

**The broader gap this exposed.** Part 2 cites specific, named third-party skills that ground its own argument: `ai-slop-cleaner` for the five-rule cleanup discipline, `holdfast` for durable partitioned execution, `sensors-cli` for a firing-history dashboard, `superpowers` and `agentskills` for the skill format itself, each one checked against `sources/inventory.md` and linked with the project's `.src` citation convention added on 31 August. Parts 3 and 4 have no equivalent. Neither cites a single concrete, installable tool for what each part actually argues: Part 3 for enforcing permission outside the model, logging, or catching prompt injection before it reaches a tool call; Part 4 for an agent registry, identity governance, or the quarterly indicator compilation the office is supposed to run. The compact guide's own MEDIR organisation does not fix this either, since its cards are not scoped per part. A reader who finishes Part 3 or Part 4 persuaded by the argument has nowhere in this project to go find a real, checked tool that helps build what the part just described, exactly the gap Part 2 does not have.

**The process to repeat, every time a skill or tool is considered for citation, not only for this round:**
1. Is it actually installed and does it pass `TOOLS.md`'s existing checklist (licence, a scan for undeclared network or execution patterns)? If not installed yet, that is the first step, not a shortcut past it.
2. Is it individually named and discussed somewhere in this project's own text, not just present inside a collection's generic card or a bullet list nobody reads closely? A skill riding along inside an installed collection is not the same claim as a skill this project actually recommends.
3. Does real usage exist to check it against, not just a read of its `SKILL.md`? A skill nobody here has actually run is a documentation claim, not a verified one.
4. Which part's argument does it actually ground, concretely, the same way `holdfast` grounds Part 2's partitioned-execution discussion? A tool cited without a specific claim it supports is decoration, not evidence.

**Done when:** `research` has its own citation, in the compact guide and, if the fit is real and not forced, in Part 2. At least one concrete, checked, named skill or tool is cited by Part 3 for its own argument, and at least one for Part 4, each passing all four checks above. `sources/inventory.md` and `TOOLS.md` updated to match whatever gets added.

**The Part 3 and Part 4 half met, 31 August 2026, via a different route than originally planned.** Rather than adding one tool citation apiece, the session that closed this ran a six-axis adversarial research pass across both parts first (information security, cryptography and identity, GDPR, the EU AI Act, LGPD, monitoring, and governance structure), dispatching one background research agent per axis with live web search, then cross-checked the result against a second, independently produced research and edit package from the author's own other AI tools. The two converged on the same corrections almost line for line, the kind of independent cross-validation this project's own citation-correction rounds have used before. The full findings live in `docs/research-p3-p4-stress-test-2026-08-31.md`, kept as the research record; the corrections actually applied went straight into `build/body_p3_en.html` and `build/body_p4_en.html`, English only so far, PT and ES to follow in a second pass. The most consequential single finding: Part 3's Europe subsection stated the EU AI Act's high-risk deadline delay "has not become law," which had become false by the time of the article's own present, the delay had been enacted as Regulation (EU) 2026/1744 five weeks earlier. Other corrections closed real citation gaps (naming RFC 8693 and SPIFFE/SPIRE next to the "short-lived scoped credential" recommendation, naming Claude Code and Cursor as the products behind the three CVEs in the supply-chain section, replacing "per industry reporting" with Brazil's actual Law 15,352/2026 for the ANPD's independence), fixed two misattributed blockquotes (the OpenTelemetry line is Aryan Kargwal's, the FinOps line is N-iX's), and corrected the IIA Three Lines Model's revision dates. `harness-toolkit.html` gained two new MEDIR-adjacent sections, Secure and Govern, sections 8 and 9, closing the tool-citation gap this item originally asked for: Secure covers short-lived credentials (RFC 8693, SPIFFE/SPIRE), Sigstore/Rekor for tamper-evident records, the Microsoft Agent Governance Toolkit, and the OWASP Agentic Skills Top 10; Govern covers Langfuse for cost-per-task tracking, the same Microsoft toolkit read a second time for its life-cycle half (flagged with a caveat: it automates the exact ungated decommissioning transition Part 4 warns against), and the commercial kill-switch landscape (ServiceNow, Cisco post-Astrix). Existing sections renumbered 10 through 14 to make room; every cross-reference and the table of contents were updated to match, and the diagnosis table's stale "Part 3, not yet published" row, a separate bug the research pass surfaced, was fixed at the same time. `research`'s own citation is still open, tracked as the one remaining item below.

**A second, narrower gap surfaced right after: nothing in Secure covered the data itself, only the agent's identity and the runtime enforcing policy around it.** Raised directly by the author while reviewing the research above: identity, credential and audit-trail entries all answer who the agent is and whether its actions are witnessed, none of them ask what the data itself is or whether it should have been touched at all. A dedicated research pass (live search, the same V/P discipline) found two tracks real and current enough to cite: PII detection and redaction (Presidio, which quietly moved from Microsoft to an independent organisation, Data Privacy Stack, in 2026 with no break in maintenance, alongside Google Model Armor and AWS Bedrock Guardrails as cloud-native alternatives) and data classification as an access boundary (Microsoft Purview, Google Model Armor/Sensitive Data Protection, AWS Macie plus Bedrock Guardrails), the last one carrying a real, documented limitation worth stating outright: Purview's own current support matrix shows zero coverage for Claude Enterprise agents. Two adjacent questions were checked and found to be genuine gaps rather than gaps to paper over: no mature tool exists yet for true data minimisation, letting an agent process personal data without storing it at all, and no standards body has published an agent-specific privacy-by-design framework, ISO/IEC 27701 became independently certifiable in October 2025 but carries no agent-specific clause, and NIST's own privacy framework update remains a draft. Both entries went into `harness-toolkit.html`'s Secure section, with the two honest gaps stated in prose rather than filled with something that only sounds like an answer; Part 3 §4 gained one sentence naming the same three tools as the answer to a question the section's own identity argument raises but does not resolve on its own, who the agent is does not say what it may touch.

**The two open verification questions the research dossier left unresolved were both closed the same day.** Article 73: the amending regulation's own text, opened directly on EUR-Lex, limits the deferral to Chapter III's first three sections, where article 26 lives; article 73 sits in Chapter IX, never named in that clause. The article 26/73 pages' apparent disagreement, flagged earlier as a possible site inconsistency, was not a bug, each page was correct for its own chapter. Part 3 §8 now states this plainly, with one dissenting secondary reading (verifywise.ai, uncited to any provision) noted rather than dropped. BlueRock's registry: the "12,000+/33%" figure is real and confirmed open on 31 August 2026 at `mcp-trust.com`, the same continuous measurement grown to a larger sample, not a figure the registry contradicted, the earlier 404 was simply the wrong URL path. `sources/inventory.md` and `harness-sources.html`, all three languages, updated to state this as resolved rather than as an open snapshot.

**A full link-and-URL audit of the day's work, requested directly by the author, found one real error worth its own entry.** Every cross-file citation link to `harness-glossary.html` and `harness-sources.html` across all five trilingual pages was checked programmatically against the ids that actually exist in each target file, in each language, catching a class of break the build scripts' own `broken: []` check cannot see (it only validates anchors within the same rendered document, never a `href="harness-sources.html#..."` pointing across files): 430 real citation links checked, zero broken. All 39 external URLs added this session were then opened live in two passes; 37 confirmed the project's claim exactly, and one real, load-bearing error surfaced: the claim that Microsoft's Agent Governance Toolkit "automates decommissioning with no owner or certifier sign-off required" overstated what its own source code does. A direct read of the actual files (not the tutorial's prose, which a first pass over-trusted) found the toolkit automatically detects decommission and orphan candidates by inactivity and heartbeat timeout, but the functions that actually change state are separate, explicit calls nothing in the toolkit invokes on its own, though those calls also do not themselves check for an approval record. Corrected everywhere the claim appeared, Part 4 §3, the compact guide's Secure and Govern sections, and `harness-sources.html`, in all three languages, reframed as an empty hook the toolkit leaves unbuilt rather than a shipped failure. The toolkit's open-source date and star count, also stale (2 April 2026 / 3,300+), were corrected to the verified 2 March 2026 / over 6,000.

**Item 4 closed in full, 31 August 2026.** Two last pieces. `research` gained its own six-field entry in the compact guide's Inspect section, the fit turned out natural rather than forced: the section's own opening line is "the step that produces evidence instead of opinion," exactly what the skill does. `TOOLS.md`, in all three languages, gained the first real entry in its own "Real usage log," which had said "no usage logged yet" since 30 August despite the skill already being used for real, several times, across this session's citation-correction work, a gap in the project's own record-keeping closed alongside the citation gap it was raised to fix. Separately, the four sources the Secure section's two data-protection entries only ever linked to inline (Presidio, Microsoft Purview, Google Model Armor, AWS Bedrock Guardrails) were added to the compact guide's own bibliography, in all three languages, closing a small consistency gap that existed in English too, not only in the translations.

---

## 5. Extract the project log's metrics engine into its own skill, done

Raised via reader feedback, 13 September 2026, comparing this project's own logbook machinery to how `intake-briefing` was pulled out of this repository into its own standalone, installable skill on 30 August 2026. Closed 13 to 14 September 2026.

**What already justified this, not just an analogy.** `build/generate_logbook_metrics.py` (239 lines) was already reused, informally, in an unrelated confidential client project on 11 September 2026, and that reuse found three real bugs fixed here as a direct result: token double-counting, a leaked local path, and the glossary-order gap (see 11 September's entry above). That is real, validated evidence the metrics engine generalises beyond this repository, the same bar `intake-briefing` had already cleared before it moved.

**What did not carry the same evidence, and was not assumed.** `build/build_logbook.py` (922 lines, the trilingual page template, the shared topbar, the `SERIES` JavaScript object) is deeply specific to this repository's own shape and was never generalised in that same reuse. The new skill follows `intake-briefing`'s own precedent, a process that produces a data artefact, not a themed HTML page a consumer would have to fight to restyle.

**The design question this raised, closed rather than assumed.** Should such a skill also draft the milestone prose, or only compute the numbers? Answered by what actually shipped: the engine computes one row per git commit (words, lines, tokens, cost), never a curated, multi-commit narrative milestone. A decision note is optional and one line, written by hand into `notes.json`, never auto-drafted. This keeps the no-inflation discipline this project's own diary already holds itself to, and it is also why the new skill's per-commit table is a different, complementary artefact from this repository's own `docs/logbook.html`, not a drop-in replacement for it.

**What was built.** Two research rounds first, verified before any design decision: `docs/research-logbook-skill-extraction.pt.md` searched five axes (agent-decision governance, LOC counters, LLM token-cost trackers, changelog generators, cross-provider price comparison) and found no mature repository covering the actual combination, the recommendation was to build, extending this project's own already-validated engine; `docs/research-agent-skill-install-paths.pt.md` verified, against each vendor's own documentation, how Claude Code, Cursor, Codex CLI, Gemini CLI, Google Antigravity and VS Code (via GitHub Copilot) each load an installable skill, later gaining one correction (the Gemini Code Assist licence caveat covers Standard or Enterprise, not Enterprise alone) found during the new repository's own final verification pass. `docs/design-milestone-loc-tokens-ai-ledger.pt.md` then specified the full architecture from that research. A fourteen-task implementation plan followed it task by task, growing to 34 real commits along the way, with genuine gains the original plan did not ask for: a live cost chart, a sources-consulted panel, cross-linked price sliders, self-hosted thumbnail cards, and a `--linked-project` flag for the case where a project's own build history lives in a different top-level session's transcripts, exactly the shape of this project's own subagent-driven build of it. The new repository ran its own engine against its own history as its own proof, the same discipline this project's "This project also inspects itself" section already applies; the published dashboard shows 34 milestones, 81.4 million tokens, $44.07 recorded cost, zero left unpriced.

**A correction to the design specification's own closing checklist, found while integrating back.** Section 12 of `docs/design-milestone-loc-tokens-ai-ledger.pt.md` named a `sources/inventory.md` entry as part of bringing the finished skill back into this project, alongside `TOOLS.md`, `toolkit.json` and the compact guide. Checked against the actual precedent it names, `intake-briefing`, before adding one: `intake-briefing` has never had an entry in `sources/inventory.md`, whose "Tools and skills" table is scoped to third-party citations only. No entry added for the new skill either, matching the real pattern rather than the plan's unchecked assumption.

**Integrated back, 14 September 2026.** `TOOLS.md`, in all three languages: the project's own skill section (now plural) gained a full paragraph, the collection count updated to 39 active skills, a local copy installed at `.claude/skills/milestone-loc-tokens-ai-ledger/`, same risk-accepted pattern as `intake-briefing`. `build/generate_toolkit_manifest.py`'s own `parse_own_skill` function, hardcoded for exactly one skill, was generalised into `parse_own_skills` before `toolkit.json` could gain the second entry, the same discipline that already governs every other part of this generator: never hand-edit the output, extend the script that derives it. `harness-toolkit.html` gained a sixth Inspect-section entry, all three languages, cross-linked to `intake-briefing`'s own card; rebuilt with zero broken links, confirmed idempotent by running the build twice.

**Done when it is actually picked up:** the metrics engine (word, line and token counting, deduplication, and the three portability gaps `build/README.md` already names: the session-directory suffix, the hardcoded file lists, and the dedup logic itself) separated cleanly from this repository's own page-rendering code; a decision made and stated on the milestone-narrative question above; its own public repository, MIT, `AGENTS.md`, `llms.txt`, multi-tool install, the same pattern `intake-briefing` already proved. **Met in full, 14 September 2026.**

---

## Minor pending items, to decide at any point

**Real opening case.** The scene is composite. If a real anonymised case from the author's ecosystem comes up, replacing it would raise the text considerably.

**Table-of-contents box border.** It is the only box border left in the documents. Decide whether it goes, to stay consistent with the removal of the others.

**Pull-quote background in print.** Depends on whether the browser is set to print background graphics. A dependency-free alternative: a thin rule above and below the block.

**English spelling.** Currently British, now the project's default reading. If the target audience shifts toward the United States, convert it.

**Locate the rule of two's primary source. Done, 31 August 2026.** Found and read directly: Meta, *Agents Rule of Two: A Practical Approach to AI Agent Security*, 31 October 2025, `ai.meta.com/blog/practical-ai-agent-security/`. Linked in Part 3, all three languages, and in `harness-sources.html`; `sources/inventory.md` updated from **P** to **V**.

**Project-wide em-dash cleanup, found 10 September 2026. Done, 11 September 2026.** `STANDARDS.md` bans the em dash "under any circumstance, in any language." Every hit was read in context, not blindly replaced: each one turned out to be this project's own prose (a milestone description, an article sentence, a citation note), and was rewritten with whichever of comma, colon, parentheses or full stop the sentence actually called for, sometimes splitting one sentence into two. One occurrence survived on purpose: the exact published title of the IIA's July 2026 *Statement of Position*, quoted verbatim in `harness-sources.html` and its three-language build source, carries its own em dash as the publisher's own punctuation, not this project's. Every affected page was rebuilt from its `build/body_*.html` source (`harness-toolkit.html`, `harness-p3.html`, `harness-p4.html`, `harness-sources.html`), plus `docs/logbook.html` from `build/build_logbook.py`, and the plain-markdown governance files (`NEXT-STEPS.md`, `sources/inventory.md`) were fixed directly. Every rebuild confirmed zero broken links. Left out of scope on purpose: the dated research-dossier files under `docs/` and `research/` (kept as historical records of what was researched at the time, not rewritten after the fact) and the uncommitted `docs/harness-medir-revisao-pt-CONSOLIDADO-FINAL.md` reference document.
